-- Server Configuration
return {
    host = "localhost",
    port = 8080,
    maxClients = 10,
    worldSize = 100, -- chunks
    chunkSize = 16,
    worldHeight = 64,
    saveInterval = 300, -- seconds
    worldName = "default"
}